import React from "react"

describe("테스트 시작", () => {
  it("5 + 10 = 15.", () => {
    // const wrapper = render(<LabelList labels={_labels} selectedLabelsIds={_selectedLabelsIds} selectLabels={mockSelectLabels} />);
    const a = 5
    const b = 10
    expect(a + b).toBe(15)
  })
})
